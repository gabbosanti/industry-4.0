# Mosquitto configuration for lab (in-memory, allow anonymous)
listener 1883
allow_anonymous true

# Persistence disabled to avoid writing state to disk
persistence false

# Increase logging verbosity for lab debugging
log_type all
